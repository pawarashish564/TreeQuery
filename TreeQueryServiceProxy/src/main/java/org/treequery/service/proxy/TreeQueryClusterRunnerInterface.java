package org.treequery.service.proxy;

import org.apache.avro.generic.GenericRecord;
import org.treequery.cluster.Cluster;
//import org.treequery.grpc.service.TreeQueryBeamServiceHelper;
import org.treequery.proto.TreeQueryRequest;

import java.util.function.Consumer;

public interface TreeQueryClusterRunnerInterface {
/*
    public TreeQueryBeamServiceHelper.ReturnResult process(Cluster cluster, TreeQueryRequest.RunMode runMode,
                                                           TreeQueryBeamServiceHelper.PreprocessInput preprocessInput,
                                                           boolean renewCache,
                                                           long pageSize,
                                                           long page,
                                                           Consumer<GenericRecord> dataConsumer);

 */
}
