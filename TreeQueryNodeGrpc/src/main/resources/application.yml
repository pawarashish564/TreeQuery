spring:
  application:
    name: TreeQueryNodeGrpc-Service-A
server:
    port: 8082
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
    healthcheck:
      enabled: true
